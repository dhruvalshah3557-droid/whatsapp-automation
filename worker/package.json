{
  "name": "messaging-webhooks-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "Serverless webhook server replacing n8n for WhatsApp, Instagram, Facebook, TikTok, LINE and WeChat auto-replies",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/worker.test.mjs"
  }
}
